# Project Reset: D:/Android/Streamix

The goal is to delete all files and subdirectories within `D:/Android/Streamix/` to start from scratch.

## User Review Required

> [!CAUTION]
> This action will delete the **ENTIRE PROJECT**, including source code (`app/`), git history (`.git/`), IDE configuration (`.idea/`), and build artifacts.
>
> **Are you absolutely sure you want to delete everything inside `D:/Android/Streamix/`?**
> This action is irreversible.

## Proposed Changes

### Project Root

#### [DELETE] All contents of `D:/Android/Streamix/`
Including but not limited to:
- `app/` (Source code and modules)
- `.git/` (Version control history)
- `.idea/` (Project settings)
- `build/`, `.gradle/`, `gradle/` (Build system)
- `Scrapers/`, `Profile icons/` (Extra assets/configs)
- `build.gradle.kts`, `settings.gradle.kts`, etc. (Build scripts)

## Verification Plan

### Manual Verification
- Run `ls D:/Android/Streamix/` to confirm the directory is empty.
